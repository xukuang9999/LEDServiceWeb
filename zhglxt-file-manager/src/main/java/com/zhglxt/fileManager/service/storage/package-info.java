/**
 * Storage abstraction layer.
 * 
 * This package provides the storage abstraction layer that allows the file manager
 * to work with different storage backends (local file system, cloud storage, etc.)
 * through a common interface.
 * 
 * @author zhglxt
 */
package com.zhglxt.fileManager.service.storage;